# OpenAI Parser Eval

## 1. Purpose

This document describes a lightweight evaluation workflow for the AI parser.

The goal is to check how well the parser converts raw Ukrainian task text into a structured task payload.

The eval is useful for both providers:

```text
AI_PROVIDER=mock
AI_PROVIDER=openai